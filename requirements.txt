#If we use tools like Poetry, we could also not include a requirements.txt file because Poetry does it itself

kivy==2.3.0
pytest>=8.4
pyperclip>=1.9
mypy_extensions>=1.1
mypy>=1.17

mysql-connector-python==9.3.0
rich>=14.0.0
cryptography>=45.0.5
gem>=0.1
pandas>=2.2
pycryptodome>=3.23
ttkbootstrap>=1.14
