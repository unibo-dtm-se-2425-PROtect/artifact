import unittest
from unittest.mock import patch, MagickMock
import string
import hashlib

from code.config import checkConfig, config, generateDeviceSecret


def test_checkConfig():
    assert checkConfig()==True 